---
import GenericButton from "../../input/button/GenericButton.astro";
import GenericSelect from "../../input/GenericSelect.astro";
import Grid from "../../icon/Grid.astro";
import List from "../../icon/List.astro";
import PageSelect from "./PageSelect.astro";
---

<section class="header">
  <div class="page">
    <PageSelect></PageSelect>
  </div>
  <div class="view">
    <GenericSelect>
      <option value="0">A-Z</option>
      <option value="1">Top rated</option>
      <option value="2">Recently added</option>
    </GenericSelect>
    <GenericButton><Grid/></GenericButton>
    <GenericButton><List/></GenericButton>
  </div>
</section>

<style lang="scss">
  .header {
    z-index: 5;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 10px 12px;
    background-color: var(--color-001);
    outline: var(--pane-outline) solid var(--color-003);
  }

  .page {
    height: 100%;
    * {
      color: var(--color-004);
      font-weight: 600;
    }
  }

  .view {
    display: flex;
    gap: 10px;
  }

  svg {
    height: 30px;
    width: auto;
  }

  button:has(svg) {
    padding: .3em;
  }
  
  button {
    font-size: 1.1em;
    --button-text-color: var(--color-005);
  }

  h2 > svg {
    display: inline;
  }
</style>
